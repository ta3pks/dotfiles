---
phase: 02-documentation-organization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - nvim/README.md
  - zsh/README.md
  - sway/README.md
  - opencode/README.md
  - kmonad/README.md
  - git/README.md
autonomous: true
requirements:
  - ORG-02

must_haves:
  truths:
    - "User can understand each tool's configuration by reading its per-tool README"
    - "User can identify dependencies required for each tool"
    - "User can customize tool configurations based on documentation"
  artifacts:
    - path: "nvim/README.md"
      provides: "Neovim configuration documentation"
      contains: "## Overview, ## Files, ## Dependencies"
    - path: "zsh/README.md"
      provides: "Zsh configuration documentation"
      contains: "## Overview, ## Files, ## Dependencies"
    - path: "sway/README.md"
      provides: "Sway configuration documentation"
      contains: "## Overview, ## Files, ## Dependencies"
  key_links:
    - from: "README.md"
      to: "per-tool READMEs"
      via: "markdown links in Structure section"
---

<objective>
Create per-tool README files for key tool directories following a consistent template.

Purpose: Enable users to understand individual tool configurations without reading raw config files.
Output: README.md files in nvim/, zsh/, sway/, opencode/, kmonad/, git/ directories.
</objective>

<execution_context>
@/home/nikos/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/nikos/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-documentation-organization/02-CONTEXT.md
@.planning/phases/02-documentation-organization/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create per-tool README template and implement for nvim, zsh, sway</name>
  <files>nvim/README.md, zsh/README.md, sway/README.md</files>
  <action>
    Create README.md files for nvim, zsh, and sway using this template:

    ```markdown
    # [Tool Name]

    [Brief one-liner description]

    ## Overview
    [What this tool does, why it's configured this way, key features]

    ## Files
    | File | Purpose |
    |------|---------|
    | `.config/[tool]/init.lua` | Main entry point |
    | `.config/[tool]/lua/plugins/` | Plugin configurations |
    [List actual files in the directory]

    ## Dependencies
    - [Required package] - [why needed]
    - [Optional package] - [why nice to have]

    ## Customization
    [How to modify for personal use - key files to edit, common changes]
    ```

    **nvim/README.md specifics:**
    - Overview: LazyVim-based config, migrated June 2025, Lua with lazy.nvim
    - Dependencies: neovim 0.9+, git, ripgrep (for telescope)
    - Files: List init.lua, lua/config/, lua/plugins/
    - Customization: Add plugins in lua/plugins/, keymaps in lua/config/keymaps.lua

    **zsh/README.md specifics:**
    - Overview: Oh My Zsh with custom aliases, shared with bash via .bash_aliases
    - Dependencies: zsh 5.x+, oh-my-zsh
    - Files: .zshrc, .bash_aliases
    - Customization: Add aliases to .bash_aliases (shared with bash)

    **sway/README.md specifics:**
    - Overview: Wayland compositor config, integrates with waybar, gammastep
    - Dependencies: sway, waybar, gammastep, fuzzel (launcher)
    - Files: config, config.d/ (modular includes)
    - Customization: Edit config for outputs, keybindings in config.d/
  </action>
  <verify>
    <automated>test -f nvim/README.md && test -f zsh/README.md && test -f sway/README.md</automated>
    <manual>Verify each README has Overview, Files, Dependencies, Customization sections</manual>
  </verify>
  <done>README.md files exist in nvim/, zsh/, sway/ with complete template sections</done>
</task>

<task type="auto">
  <name>Task 2: Create per-tool READMEs for opencode, kmonad, git</name>
  <files>opencode/README.md, kmonad/README.md, git/README.md</files>
  <action>
    Create README.md files using the same template:

    **opencode/README.md specifics:**
    - Overview: Primary AI coding environment, migrated to micode framework Sept 2025
    - Dependencies: bun (for plugins), ollama (optional, for local models)
    - Files: .config/opencode/config.json, .config/opencode/AGENTS.md, .config/opencode/commands/
    - Key features: Multi-model providers (ZAI, Ollama, OpenRouter), MCP servers (serena, nvim, clickhouse)
    - Customization: Add MCP servers in config.json, custom commands in commands/

    **kmonad/README.md specifics:**
    - Overview: Advanced keyboard remapping with multitap and layer switching
    - Dependencies: kmonad binary, udev rules for input device access
    - Files: .config/kmonad/config.kbd
    - Key features: Home row mods, multitap sequences, multiple layers
    - Customization: Edit config.kbd for key mappings, layers

    **git/README.md specifics:**
    - Overview: Git configuration with aliases and credential helper
    - Dependencies: git 2.x+
    - Files: .gitconfig, .gitignore (global)
    - Key features: Useful aliases, credential caching, diff highlighting
    - Customization: Edit .gitconfig for name/email, add aliases
  </action>
  <verify>
    <automated>test -f opencode/README.md && test -f kmonad/README.md && test -f git/README.md</automated>
    <manual>Verify each README follows template and has accurate content</manual>
  </verify>
  <done>README.md files exist in opencode/, kmonad/, git/ with complete template sections</done>
</task>

</tasks>

<verification>
- All ORG-02 requirements addressed
- 6 per-tool README files created
- Each README has: Overview, Files, Dependencies, Customization
- Content accurately reflects actual configurations
</verification>

<success_criteria>
- User can understand each tool's configuration by reading its README
- User can identify dependencies for each tool
- User knows how to customize each tool
</success_criteria>

<output>
After completion, create `.planning/phases/02-documentation-organization/02-02-SUMMARY.md`
</output>
