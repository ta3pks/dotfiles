---
phase: 02-documentation-organization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03, ORG-03]

must_haves:
  truths:
    - "User can read Quick Start and know how to install in one line"
    - "User can read Structure section and understand directory organization"
    - "User can read Prerequisites and know what to install before setup"
    - "User can read Symlink Strategy and understand the XDG-compliant approach"
  artifacts:
    - path: "README.md"
      provides: "Main documentation entry point"
      min_lines: 100
      contains: ["## Quick Start", "## Prerequisites", "## Structure", "## Symlink Strategy"]
  key_links:
    - from: "README.md"
      to: "per-tool READMEs"
      via: "markdown links"
      pattern: "\\[.*\\]\\([a-z]+/README\\.md\\)"
---

<objective>
Create comprehensive README.md with all required sections for documentation requirements.

Purpose: Single source of truth for repository understanding, installation, and structure.
Output: Enhanced README.md with Quick Start, Prerequisites, Key Components, Installation, Structure, Symlink Strategy, Secrets Management, Contributing sections.
</objective>

<execution_context>
@/home/nikos/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/nikos/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-documentation-organization/02-CONTEXT.md
@.planning/phases/02-documentation-organization/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Enhance README with comprehensive sections</name>
  <files>README.md</files>
  <action>
Rewrite README.md following the structure from CONTEXT.md and RESEARCH.md:

**Required sections (in order):**
1. **Title + 1-2 sentence description** - Keep existing
2. **Quick Start** - ONE line only: `git clone https://github.com/ta3pks/dotfiles.git ~/dotfiles && cd ~/dotfiles && ./install` (reference future install script)
3. **Prerequisites** - Bulleted list with categories:
   - Shell: zsh, oh-my-zsh
   - Editor: neovim 0.9+, git
   - Optional: gum (for install script TUI), gitleaks (for secret scanning)
4. **Key Components** - Keep existing table, add more entries for: ghostty, kanata, espanso, git, opencode, kitty
5. **Installation** - Reference the interactive install script (Phase 3):
   - `./install` runs interactive TUI
   - `./install --dry-run` previews changes
   - `./install --undo` restores backups
   - Explain symlink approach (manual, no stow)
6. **Structure** - Directory tree with one-liners for top-level dirs:
   ```
   ~/dotfiles/
   ├── nvim/          # Neovim (LazyVim)
   ├── zsh/           # Shell configuration
   ├── sway/          # Wayland compositor
   ├── opencode/      # AI coding environment
   ├── kmonad/        # Keyboard remapping
   ├── git/           # Git configuration
   ├── espanso/       # Text expansion
   ├── ghostty/       # Terminal emulator
   └── ...
   ```
   Link to per-tool READMEs: `See [nvim/README.md](nvim/README.md) for details.`
7. **Symlink Strategy** - New section explaining:
   - XDG Base Directory compliance (`~/.config/<tool>/`)
   - Manual symlinks over GNU Stow (why: explicit control, no dependency)
   - Mapping table example (source → destination)
   - Reference to per-tool READMEs for specific mappings
8. **Secrets Management** - Keep existing content
9. **Contributing** - Brief section:
   - "Personal dotfiles, but feel free to borrow ideas"
   - Link to ADRs in docs/decisions/ for architectural decisions

**Tone:** Technical, concise, assumes Linux familiarity (per CONTEXT.md)
**Audience:** Future you - document WHY, not just WHAT
</action>
  <verify>
    <automated>grep -q "## Quick Start" README.md && grep -q "## Prerequisites" README.md && grep -q "## Structure" README.md && grep -q "## Symlink Strategy" README.md && echo "README sections verified"</automated>
  </verify>
  <done>README.md contains all required sections with proper formatting and content</done>
</task>

</tasks>

<verification>
- [ ] README.md has all 8 required sections
- [ ] Quick Start is one line
- [ ] Prerequisites lists required tools
- [ ] Structure section has directory tree
- [ ] Symlink Strategy explains XDG compliance
- [ ] Links to per-tool READMEs work
</verification>

<success_criteria>
- `grep -c "^## " README.md` returns 8+ (all sections present)
- README.md is >100 lines
- All section headers from CONTEXT.md present
</success_criteria>

<output>
After completion, create `.planning/phases/02-documentation-organization/02-01-SUMMARY.md`
</output>
