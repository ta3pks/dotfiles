---
phase: 02-documentation-organization
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [docs/decisions/000-template.md, docs/decisions/001-manual-symlinks.md, docs/philosophy.md, README.md]
autonomous: true
requirements: [ORG-01]

must_haves:
  truths:
    - "User can find ADRs explaining key architectural decisions"
    - "User can understand the philosophy behind tool selection and organization"
    - "User can understand XDG compliance approach"
  artifacts:
    - path: "docs/decisions/000-template.md"
      provides: "ADR template for future decisions"
      contains: ["## Context", "## Decision", "## Consequences"]
    - path: "docs/decisions/001-manual-symlinks.md"
      provides: "First ADR documenting symlink approach"
      min_lines: 20
    - path: "docs/philosophy.md"
      provides: "Repository philosophy and principles"
      min_lines: 30
  key_links:
    - from: "README.md"
      to: "docs/decisions/"
      via: "Contributing section link"
      pattern: "docs/decisions/"
---

<objective>
Create supporting documentation: ADR template, first ADR, philosophy doc, and XDG compliance documentation.

Purpose: Document architectural decisions and philosophy for future reference.
Output: docs/decisions/ directory with ADRs, docs/philosophy.md, README.md updates for XDG section.
</objective>

<execution_context>
@/home/nikos/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/nikos/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-documentation-organization/02-CONTEXT.md
@.planning/phases/02-documentation-organization/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create ADR directory and template</name>
  <files>docs/decisions/000-template.md</files>
  <action>
Create docs/decisions/ directory and ADR template using MADR-lite format from RESEARCH.md:

```markdown
# ADR Template

Use this template for recording architectural decisions.

---

# [NNN]. [Decision Title]

Date: YYYY-MM-DD

## Context

[Why this decision was needed. What is the issue or problem being addressed?]

## Decision

[What was decided. Be specific and concrete.]

## Consequences

[Impact of this decision - both positive and negative. What becomes easier or harder?]

---

## ADR Index

| Number | Title | Date |
|--------|-------|------|
| 001 | Manual Symlinks over GNU Stow | 2026-03-11 |
```

Use sequential numbering (001, 002, ...) per RESEARCH.md recommendation.
</action>
  <verify>
    <automated>test -f docs/decisions/000-template.md && echo "ADR template exists"</automated>
  </verify>
  <done>docs/decisions/000-template.md created with MADR-lite structure</done>
</task>

<task type="auto">
  <name>Create first ADR for manual symlinks decision</name>
  <files>docs/decisions/001-manual-symlinks.md</files>
  <action>
Create first ADR documenting the decision to use manual symlinks instead of GNU Stow:

```markdown
# 001. Manual Symlinks over GNU Stow

Date: 2026-03-11

## Context

This dotfiles repository needs a strategy for linking configuration files from the repository to their expected locations in the home directory. Common approaches include:
- GNU Stow (automatic symlink management)
- chezmoi (templated dotfiles manager)
- yadm (yet another dotfiles manager)
- Manual symlinks (explicit `ln -s` commands)

The repository already uses manual symlinks and has 30+ tool directories.

## Decision

We will use **manual symlinks** with an interactive install script, not GNU Stow or other dotfiles managers.

## Consequences

**Positive:**
- Full control over what gets linked and where
- No external dependency (stow, chezmoi, etc.)
- Explicit and transparent - easy to understand what's happening
- Works on any Unix system without additional packages
- Easy to handle edge cases (different destinations, conditional linking)

**Negative:**
- More initial setup compared to `stow */`
- Install script needs to be maintained
- No built-in templating for machine-specific configs

**Neutral:**
- XDG Base Directory compliance is maintained through consistent destination paths
```
</action>
  <verify>
    <automated>test -f docs/decisions/001-manual-symlinks.md && grep -q "## Decision" docs/decisions/001-manual-symlinks.md && echo "First ADR created"</automated>
  </verify>
  <done>ADR 001 documents the manual symlinks decision with context, decision, and consequences</done>
</task>

<task type="auto">
  <name>Create philosophy document</name>
  <files>docs/philosophy.md</files>
  <action>
Create docs/philosophy.md documenting the principles behind this dotfiles repository:

```markdown
# Dotfiles Philosophy

## Core Principles

### 1. Explicit over Implicit

Manual symlinks over GNU Stow. Explicit `ln -s` commands over magic. 
I want to know exactly what's happening to my system.

### 2. XDG Compliance

Configuration follows the [XDG Base Directory Specification](https://specifications.freedesktop.org/basedir-spec/latest/):
- Configs live in `~/.config/<tool>/`
- Data lives in `~/.local/share/<tool>/`
- Cache lives in `~/.cache/<tool>/`

This keeps the home directory clean and makes backups easier.

### 3. Documentation for Future Me

I assume I'll forget everything. Every non-obvious decision is documented:
- Inline comments for small things
- ADRs for architectural choices
- Per-tool READMEs for configuration details

### 4. No Secrets in Version Control

API keys, tokens, and credentials never enter this repository:
- Use `.env` files (gitignored)
- Use a password manager
- Pre-commit hooks scan for secrets

### 5. Tool Selection Criteria

Tools are chosen based on:
- Active maintenance
- Community adoption
- Configuration flexibility
- CLI-first (GUI configs defeat the purpose)

### 6. Minimal Dependencies

The install script should work with standard Unix tools. Optional enhancements (gum for TUI) gracefully degrade.

## Organization

```
~/dotfiles/
├── <tool>/           # One directory per tool
│   ├── .config/      # XDG config (symlinked to ~/.config/<tool>/)
│   └── README.md     # Tool-specific documentation
├── docs/             # Cross-cutting documentation
│   └── decisions/    # Architecture Decision Records
└── README.md         # Main entry point
```

## Why Not Stow?

See [ADR-001: Manual Symlinks over GNU Stow](decisions/001-manual-symlinks.md).
```
</action>
  <verify>
    <automated>test -f docs/philosophy.md && grep -q "## Core Principles" docs/philosophy.md && echo "Philosophy doc created"</automated>
  </verify>
  <done>docs/philosophy.md documents core principles, XDG compliance, tool selection criteria</done>
</task>

<task type="auto">
  <name>Add XDG compliance section to README</name>
  <files>README.md</files>
  <action>
Add XDG compliance details to the Symlink Strategy section in README.md (created in Plan 01).

In the Symlink Strategy section, add:

```markdown
### XDG Base Directory Compliance

This repository follows the [XDG Base Directory Specification](https://specifications.freedesktop.org/basedir-spec/latest/):

| XDG Path | Purpose | Example |
|----------|---------|---------|
| `~/.config/` | Configuration files | `~/.config/nvim/` |
| `~/.local/share/` | Data files | `~/.local/share/nvim/` |
| `~/.cache/` | Cache files | `~/.cache/nvim/` |

**Repository structure mirrors XDG:**
- `nvim/.config/nvim/` → `~/.config/nvim/`
- `sway/.config/sway/` → `~/.config/sway/`

See [docs/philosophy.md](docs/philosophy.md) for the full philosophy.
```

Also update Contributing section to link to ADRs:
```markdown
## Contributing

Personal dotfiles, but feel free to borrow ideas. For architectural decisions, see [docs/decisions/](docs/decisions/).
```
</action>
  <verify>
    <automated>grep -q "XDG Base Directory" README.md && grep -q "docs/decisions/" README.md && echo "XDG and ADR links added to README"</automated>
  </verify>
  <done>README.md includes XDG compliance section and links to docs/</done>
</task>

</tasks>

<verification>
- [ ] docs/decisions/000-template.md exists
- [ ] docs/decisions/001-manual-symlinks.md exists
- [ ] docs/philosophy.md exists
- [ ] README.md has XDG section
- [ ] README.md links to docs/decisions/
</verification>

<success_criteria>
- docs/ directory created with decisions/ subdirectory
- 2 ADR files exist (template + first ADR)
- philosophy.md has 6+ core principles
- README.md references XDG spec and ADRs
</success_criteria>

<output>
After completion, create `.planning/phases/02-documentation-organization/02-03-SUMMARY.md`
</output>
