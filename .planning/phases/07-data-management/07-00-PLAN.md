---
phase: 7
plan: 07-00
type: implementation
wave: 0
depends_on: []
files_modified:
  - memory/tests/cli/helpers/setup.ts
  - memory/tests/cli/helpers/setup.test.ts
  - memory/src/core/service.ts
  - memory/tests/core/service.test.ts
autonomous: true
requirements: []
status: complete
completed_at: 2026-03-12T21:35:00Z
must_haves:
  - "tests/cli/helpers/setup.ts provides shared test fixtures for CLI tests"
  - "helper creates isolated test database with predictable memories"
  - "helper provides cleanup function to remove test artifacts"
---

# Plan 07-00: Wave 0 Test Helper Setup

## Goal
Create shared test infrastructure for CLI command tests in Phase 7.

## Context
- Tests for export, import, backup, and cleanup commands need consistent fixtures
- Helper provides isolated test environment with predictable memory data
- Pattern follows existing test utilities from tests/cli/store.test.ts

## Tasks

### Task 1: Create Test Helper
```xml
<task type="auto">
  <name>Create Test Helper for CLI Tests</name>
  
  <files>
    tests/cli/helpers/setup.ts
  </files>
  
  <action>
    1. Create tests/cli/helpers/ directory if not exists
    2. Create setup.ts with the following exports:
       - setupTestDb(): Promise<{ dbPath: string; cleanup: () => Promise<void> }>
         - Creates isolated temporary directory for test database
         - Initializes memory service with test config
         - Returns cleanup function to remove test artifacts
       - createTestMemories(count: number, options?: { tags?, project? }): Promise<MemoryEntry[]>
         - Creates N test memories with predictable content
         - Optional tags and project assignment
         - Returns array of created memories
       - assertFileExists(path: string): void
         - Throws if file doesn't exist
       - assertJsonFileValid(path: string): Promise<object>
         - Reads and parses JSON file
         - Returns parsed object
         - Throws if invalid JSON
    3. Use bun:test for test utilities
    4. Follow existing patterns from tests/cli/store.test.ts
  </action>
  
  <verify>
    <automated>bun test tests/cli/helpers/setup.test.ts</automated>
    <manual>Import helper in a test file and verify exports are accessible</manual>
  </verify>
  
  <done>
    - tests/cli/helpers/setup.ts exists with all exports
    - setupTestDb creates isolated test environment
    - createTestMemories generates predictable test data
    - Helper can be imported by other test files
  </done>
</task>
```

### Task 2: Create Helper Self-Test
```xml
<task type="auto">
  <name>Create Helper Self-Test</name>
  
  <files>
    tests/cli/helpers/setup.test.ts
  </files>
  
  <action>
    1. Create tests/cli/helpers/setup.test.ts
    2. Test: setupTestDb creates temp directory and returns cleanup
    3. Test: cleanup removes temp directory
    4. Test: createTestMemories creates N memories with defaults
    5. Test: createTestMemories accepts tags and project options
    6. Test: assertFileExists passes for existing file
    7. Test: assertFileExists throws for missing file
    8. Test: assertJsonFileValid parses valid JSON
    9. Test: assertJsonFileValid throws for invalid JSON
  </action>
  
  <verify>
    <automated>bun test tests/cli/helpers/setup.test.ts</automated>
  </verify>
  
  <done>
    - All helper self-tests pass
    - Helper functions verified to work correctly
  </done>
</task>
```

## Dependencies
- None (Wave 0 - foundation for other plans)

## Success Criteria Met
- [x] Test helper provides shared fixtures for CLI tests
- [x] Helper self-tests verify correct behavior
