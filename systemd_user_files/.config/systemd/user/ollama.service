[Unit]
Description=Ollama Service
After=network.target

[Service]
Restart=on-failure
ExecStart=/usr/bin/ollama serve

# Optionally, set environment variables, e.g., for OLLAMA_HOST
#Environment=OLLAMA_HOST=0.0.0.0:11434

[Install]
WantedBy=default.target
